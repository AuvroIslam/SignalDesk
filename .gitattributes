# Keep text files LF in the repository, checked out natively per platform.
* text=auto eol=lf

# Binary assets must never be line ending converted.
*.png binary
*.ttf binary
*.jks binary
*.keystore binary
